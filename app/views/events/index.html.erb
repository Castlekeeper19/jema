<div class="banner events-banner nav-fix">
  <h1 >Events:</h1>
  <div>
    <%= link_to "Add Event", new_event_path, class: "btn btn-ghost"  %>
    <%= link_to "Log In   ",new_user_session_path, class: "btn btn-ghost" %>
  </div>
</div>
<div class = "events-container">
  <h2>JEMA FEATURED EVENTS</h2>
  <div class= "event-cards">
    <% @events.each do |event| %>
    <%= link_to event_path(event), class: "event-card" do  %>
      <img src="<%= event.photo.attached? ? cl_image_path(event.photo.key, width: 150, height: 150) : image_path("https://source.unsplash.com/150x150/?#{event.location}") %>" class="event-photo">
      <div class= "event-info">
        <h2><%= event.name %></h2>
        <h4 class="text-secondary"><%= event.location %></h4>
        <p class="text-primary"> <%= event.church.name %> </p>
        <p ><i><%= event.start.strftime("%B #{event.start.day.ordinalize}, %-l:%M %p") %></i></p>
      </div>
    <% end %>
    <% end %>
  </div>
</div>

